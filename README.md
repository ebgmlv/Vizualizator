# Практическая работа №2 
## Этап 1

Этот проект — первый этап разработки инструмента визуализации графа зависимостей пакетов **без использования готовых менеджеров пакетов или сторонних библиотек для разрешения зависимостей**.

На данном этапе реализован минимальный **CLI-прототип**, который:
- принимает параметры через командную строку,
- выполняет валидацию всех входных данных,
- выводит конфигурацию в формате ключ-значение.

> **Важно**: На этом этапе **не происходит** чтения зависимостей или построения графа. Цель — только настройка и валидация параметров.

---

## 🛠 Требования

- Python 3.7 или выше
- Никаких внешних зависимостей (только стандартная библиотека)

---

## 🚀 Использование

Запустите приложение с обязательными параметрами:

```bash
python main.py \
  --package <имя_пакета> \
  --version <версия> \
  --repo <путь_или_URL> \
  --mode <режим> \
  --output <файл_вывода>
```

### Пример (режим `test` — путь не обязан существовать):

```bash
python main.py --package mylib --version 1.2.3 --repo ./test-repo --mode test --output graph.png
```

### Поддерживаемые режимы (`--mode`):
- `online` — репозиторий доступен по сети (HTTP/HTTPS)
- `offline` — локальный путь к репозиторию (должен существовать)
- `test` — тестовый режим; путь может быть виртуальным (не проверяется)

### Поддерживаемые расширения вывода (`--output`):
`.png`, `.svg`, `.pdf`, `.jpg`, `.jpeg`, `.dot`

---

##  Обработка ошибок

Приложение проверяет:
- Непустоту всех параметров
- Корректность имени пакета и версии
- Существование локального пути (в режимах `online`/`offline`)
- Валидность URL (для сетевых репозиториев)
- Поддерживаемое расширение выходного файла

При ошибке выводится понятное сообщение и завершается с кодом `1`.

---

## Этап 2: Сбор данных о зависимостях

Реализовано получение **прямых зависимостей** указанного пакета NuGet по его имени и версии, используя официальный репозиторий NuGet.  
Инструмент извлекает `.nuspec`-файл напрямую по [flat-container layout](https://learn.microsoft.com/en-us/nuget/api/package-base-address-resource), парсит его и выводит список зависимостей.

> ️ **Запрещено** использовать менеджеры пакетов (например, `nuget.exe`, `dotnet restore`) или сторонние библиотеки (вроде `NuGet.Client`). Реализация выполнена **только на стандартной библиотеке Python** (`urllib`, `xml.etree.ElementTree` и др.).

### Что реализовано:
- Автоматическое построение URL `.nuspec`-файла для `nuget.org` (а также поддержка пользовательских flat-container URL)
- Загрузка и парсинг `.nuspec` XML
- Извлечение прямых зависимостей (`<dependency id="..." version="..." />`)
- Вывод зависимостей в терминал (в формате `id (версия)`)

### Пример запуска:
```bash
python main.py --package Newtonsoft.Json --version 13.0.3 --repo https://api.nuget.org/v3/index.json --mode online --output graph.png
```

> 💡 **Примечание**: пока поддерживается только режим `online`. Режимы `offline` и `test` пропускают сбор данных и выводят уведомление.

---

## 📁 Структура проекта

```
.
├── main.py          # Основной CLI-скрипт
└── README.md        # Этот файл
```

---


# Этап 3: Построение графа зависимостей с транзитивностью

Реализовано построение полного графа зависимостей с использованием **DFS (рекурсивно)**, обнаружение циклических зависимостей и поддержка тестового режима.

## Запуск

```bash
python main.py --package <PACKAGE> --repo <PATH/URL> --mode <online|offline|test> --output <FILE>
```

> **Важно**:  
> - В `test`-режиме имена пакетов должны быть **заглавными латинскими буквами** (например, `A`, `X`).  
> - Поддерживаемые расширения вывода: `.png`, `.svg`, `.pdf`, `.jpg`, `.jpeg`, `.dot`.

---

## Примеры (тестовый режим)

### 1. Обнаружение цикла
Файл `cyclic.txt`:
```
X: Y
Y: Z
Z: X
```

```bash
python main.py --package X --repo cyclic.txt --mode test --output graph.png
```
→ Вывод:  
✅ Строит транзитивный граф `X → Y → Z → X`  
⚠️ Обнаруживает цикл: `X → Y → Z → X`

---

### 2. Ациклический граф
Файл `acyclic.txt`:
```
A: B C
B: D
C: D
D:
```

```bash
python main.py --package A --repo acyclic.txt --mode test --output graph.png
```
→ Вывод:  
Строит полный граф зависимостей от `A`:  
```
A: B C  
B: D  
C: D  
D: (none)
```  
Циклов нет.

---

## Примечания
- В `online`-режиме реализованы **только прямые зависимости** (транзитивный анализ сложен из-за версионирования и выходит за рамки этапа).  
- Циклы детектируются во время DFS при повторном входе в узел из текущего стека вызовов.  
- Недостижимые от корневого пакета компоненты (например, цикл `X-Y-Z`, если запущено от `A`) **не анализируются** — граф строится только для поддерева зависимостей корня.
```

---

**Выполнила**: Ева Богомолова, ИКБО-51-24\
**Репозиторий**: [Vizualizator](https://github.com/ebgmlv/Vizualizator)
